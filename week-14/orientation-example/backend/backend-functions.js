'use strict';

/* import fs from 'fs';

exasync function getFiledata() {
  const filepath = './data.json';

  fs.readFile(filepath, 'utf-8', (err, data) => {
    if (err) {
      throw new Error('Hibás file beolvasás!');
    }
    return fileData;

  });
} */

